# Issue 000 — Stage 0

## Goal
Set up a minimal project skeleton for Platform CRM with frontend + backend running locally.

## Definition of Done
- Frontend starts locally.
- Backend starts locally.
- `GET /health` returns `{ "status": "ok" }`.
- shadcn/ui is configured in frontend.
- Empty pages exist for `candidates`, `contacts`, `vacancies`, and `users`.
- Repository includes project documentation and startup commands.
